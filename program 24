Write a program to calculate electricity bill based on units consumed with these rates: 
First 100 units at ₹5/unit 
Next 100 units at ₹7/unit 
Next 100 units at ₹10/unit 
Above at ₹12/unit



#include <stdio.h>

int main() {
    int units;
    float bill = 0;

    printf("Enter total units consumed: ");
    scanf("%d", &units);

    if (units < 0) {
        printf("Invalid input! Units cannot be negative.\n");
        return 0;
    }

    if (units <= 100) {
        bill = units * 5;  // First 100 units
    }
    else if (units <= 200) {
        bill = (100 * 5) + (units - 100) * 7;  // Next 100 units
    }
    else if (units <= 300) {
        bill = (100 * 5) + (100 * 7) + (units - 200) * 10;  // Next 100 units
    }
    else {
        bill = (100 * 5) + (100 * 7) + (100 * 10) + (units - 300) * 12;  // Above 300 units
    }

    printf("Total Electricity Bill = ₹%.2f\n", bill);

    return 0;
}
