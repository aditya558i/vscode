WAP to compute the BMI index of the person and print the BMI values as per the following ranges. you can use the following formula to compute BMI= weight(kgs)*Height(mts).


#include<stdio.h>

int main() {
    float weight, height, bmi;

    // input weight in kilograms and the height in meters 
    printf("Enter your weight in kg: ");
    scanf ("%f", &weight);

    printf("Enter your height in meters:");
    scanf("%f", &height);

    //compute BMI
    if(bmi < 18.5) {
      printf("you are underweight.\n");
    } else if (bmi >= 18.5 && bmi < 24.9) {
      printf("you have a normal weight.\n");
    } else if (bmi >= 25 && bmi < 29.9) {
     printf("You are overweight.\n");
    } else {
       printf("you are obese.\n");
    }

    return 0;
}

    
